# T11 炮台（duo 双管）贴图 阶段产出喵

## 目标喵
为 Blockdustry 的 duo 双管炮塔把贴图补齐为 Mindustry 原版风格：转盘 `turret_top` + 双炮管 `turret_barrel_l/r`，并顺带核查分裂炮（scatter）转盘 `scatter_top` 是否需用原版 `scatter.png` 家族补全。只改贴图资产 + 写文档，不改 Java/模型喵。

## 结论/产出喵

### 1. 现状核对（改动前）喵
| 项目资产 | 尺寸 | 状态 | 来源喵 |
|---|---|---|---|
| `textures/entity/turret_top.png` | 32×32 | 已是原版 `duo.png`（逐像素一致） | 无需改喵 |
| `textures/entity/turret_barrel_r.png` | 32×32 | 已是原版 `duo-barrel-r.png`（逐像素一致） | 无需改喵 |
| `textures/entity/turret_barrel_l.png` | 32×32 | **偏离原版**：几何（alpha 蒙版）一致，但颜色为 `(184,146,127)`，原版 `duo-barrel-l.png` 为 `(143,102,91)` 深红棕 | **本次覆盖修正**喵 |
| `textures/entity/scatter_top.png` | 64×64 | 已是原版 `scatter-mid.png`（逐像素一致） | 无需改喵 |

### 2. 本次改动喵
- `src/main/resources/assets/blockdustry/textures/entity/turret_barrel_l.png` ← 覆盖为 Mindustry 原版 `duo-barrel-l.png`（32×32、RGBA、色 `(143,102,91)`），与原版左炮管深色一致。修复原左管颜色偏差，双炮管与 `duo.png` 转盘配色（浅 `(201,165,143)` / 深 `(143,102,91)`）重新统一喵。
- 未改：`turret_top.png`、`turret_barrel_r.png`、`scatter_top.png`（本就逐像素等于原版）喵。

### 3. 分裂炮（scatter）核查结论：已补全，无需再动喵
原版 `scatter` 家族对应关系已完整落地：
| 原版 | 项目资产 | 说明喵 |
|---|---|---|
| `scatter-mid.png`（64×64 单管转盘） | `textures/entity/scatter_top.png` | BER 叠画旋转转盘（`ScatterBlockEntityRenderer`）喵 |
| `scatter.png`（64×64 2×2 底座） | `textures/block/scatter_base.png` | 块模型顶面基座（已按 block 渲染需求填成全不透明，alpha 255）喵 |
| `scatter-preview.png`（64×64 预览） | — | 仅作参考图，项目内未引用喵 |

故 `scatter_top.png` 保持原版 `scatter-mid.png` 即可，无需补全喵。

### 4. 参考图归档（`D:\Blockdustry\像素图片绘制尝试\炮台\`）喵
按"命名保持"整理为两个子目录，全部与 Mindustry 原版逐像素一致：
- `双管\`：`duo.png`（转盘顶面）、`duo-barrel-l.png`（左炮管）、`duo-barrel-r.png`（右炮管）喵
- `分裂\`：`scatter.png`（底座）、`scatter-mid.png`（转盘）、`scatter-preview.png`（预览）喵

## 校验结果（PIL）喵
脚本逐像素比对：尺寸、RGBA 模式、透明底（alpha 范围 0..255，有透明背景 + 不透明像素）、MD5 与 Mindustry 原版一致喵。

| 文件 | 尺寸 | 模式 | alpha 范围 | 不透明像素/总数 | 与原版一致喵 |
|---|---|---|---|---|---|
| `turret_top.png` | 32×32 | RGBA | (0,255) | 316/1024 | ✓ 喵 |
| `turret_barrel_l.png` | 32×32 | RGBA | (0,255) | 78/1024 | ✓（本次覆盖后）喵 |
| `turret_barrel_r.png` | 32×32 | RGBA | (0,255) | 78/1024 | ✓ 喵 |
| `scatter_top.png` | 64×64 | RGBA | (0,255) | 696/4096 | ✓ 喵 |

渲染方式（`TurretBlockEntityRenderer`/`ScatterBlockEntityRenderer`）为 `RenderType.entityCutout` 平面 quad，透明底可正常显示；改后四贴图均为原版透明底 + 不透明像素，无多余绘制效果（未做旧/磨损/特效）喵。

## 改动文件清单喵
| 文件 | 动作喵 |
|---|---|
| `src/main/resources/assets/blockdustry/textures/entity/turret_barrel_l.png` | 覆盖：改为原版 `duo-barrel-l.png`（32×32 深红棕左炮管）喵 |
| `docs/子agent/T11_炮台贴图.md` | 新增：本文档喵 |
| `D:\Blockdustry\像素图片绘制尝试\炮台\双管\`、`\炮台\分裂\` | 参考图归档（与 Mindustry 原版一致）喵 |

未改：`turret_top.png`、`turret_barrel_r.png`、`scatter_top.png`、Java、模型 JSON、blockstate 喵。

## 占用与交接喵
- 占用文件：上述贴图 + 文档 + 参考图归档喵。
- 交接给：主会话。改动仅为 `turret_barrel_l.png` 配色修正，无代码改动；`runClient` 时 Gradle `processResources` 会自动把 `src/main/resources` 同步到 `build/resources/main`，直接实测 duo 双管炮塔左右炮管颜色即可（左深 `(143,102,91)`、右浅 `(201,165,143)`）喵。
- 风险/待人工排查：无。左炮管颜色修正后与转盘 `duo.png` 同色系，视觉与 Mindustry 原版一致喵。

## 异常喵
无（PIL 校验全通过）喵。
