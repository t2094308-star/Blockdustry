# ^T50 审查修复清单喵

> 依据 `^P1_批1CD_审查报告.md`「六、问题清单汇总」修复 8 个中危问题喵。
> 已用 JDK21 javac 局部编译 8 个改动 .java 全部通过（EXIT 0），未跑全量 gradle（并行锁冲突，全量由主会话统一做）喵。

## 8 个中危修复明细喵

### 1.【中】激光钻空转 57× — 已修
- `src/main/java/com/blockdustry/client/LaserDrillBlockEntityRenderer.java`
  - 原 L55 `pose.mulPose(Axis.YP.rotation(angle))`（弧度）→ 改为 `Axis.YP.rotationDegrees(angle)`（度数）
  - `angle = be.getSpin() * ROTATE_SPEED`（= timeDrilled×2°，原版 rotateSpeed=2）保持不变
  - 参考 BlastDrillBlockEntityRenderer L84 `rotationDegrees(be.getSpin()*getRotateSpeed())` 同款写法喵

### 2.【中】激光钻缺 mine item 矿团层 — 已修
- `src/main/java/com/blockdustry/building/LaserDrillBlockEntity.java`
  - 新增 `public Item getDominantItem()` 返回 dominantItem（参考 BlastDrillBlockEntity L77-79 / PneumaticDrillBlockEntity L52）
- `src/main/java/com/blockdustry/client/LaserDrillBlockEntityRenderer.java`
  - 新增 `ITEM` 纹理常量 = `textures/block/laser_drill_item.png`
  - render() 在 top 层之后补 mine item tint 层：`BlastDrillBlockEntity.oreColor(getDominantItem())` 染矿色、half=0.5（1×1 矿团，参考 pneumatic item 层）
  - 新增 `drawTintedQuad(...r,g,b)` 支持 tint；原 `drawQuad` 改为委托 drawTintedQuad 白色调用
  - 新增 import `com.blockdustry.building.BlastDrillBlockEntity`（复用 oreColor 静态方法，统一矿色表）
- 资源：新增 `src/main/resources/assets/blockdustry/textures/block/laser_drill_item.png`（复制 pneumatic_drill_item.png 的白色矿团贴图）喵

### 3.【中】硅冶炼尾焰闪烁快 20× — 已修
- `src/main/java/com/blockdustry/client/SiliconSmelterBlockEntityRenderer.java`
  - 原 L81 `float t = gameTime + partialTick;` → `float t = (gameTime + partialTick) / 20f;`
  - 照抄 kiln 渲染器 L64 的 `/20f` 秒换算，absin 周期恢复 5/8 秒喵

### 4.【中】硅冶炼缺 getRenderBoundingBox — 已修
- `src/main/java/com/blockdustry/client/SiliconSmelterBlockEntityRenderer.java`
  - 新增 `getRenderBoundingBox`：锚点 2×2 + `.inflate(2.0)`（覆盖火焰光晕/冒烟，参考 laser/pneumatic L33-38 写法）
  - 新增 import `net.minecraft.world.phys.AABB`喵

### 5.【中】窑炉 warmup 火焰不灭 — 已修
- `src/main/java/com/blockdustry/production/KilnBlockEntity.java`
  - 原 L121 `if(!producing) return;` 提前返回跳过末尾 warmup 同步块
  - 改为：craft 逻辑包进 `if (producing) {...}`，warmup 同步块移到 producing 块外（缺料/断电时 warmup 衰减也同步 → 客户端火焰熄灭）
  - 结构对齐 SiliconSmelterBlockEntity L121-142 的同款写法喵

### 6.【中】雷光命中建筑客户端滑行 — 已修
- `src/main/java/com/blockdustry/entities/FireBulletEntity.java`
  - 新增 `DATA_STOPPED` 同步位：`EntityDataAccessor<Boolean>`（Boolean 序列化）
  - defineSynchedData 增加 `builder.define(DATA_STOPPED, false)`
  - tick()：客户端分支判断 `if (!entityData.get(DATA_STOPPED)) move(...)`；服务端照常 move
  - 命中建筑时在 `setDeltaMovement(ZERO)` 处补 `entityData.set(DATA_STOPPED, true)` 同步客户端
  - 参考 DATA_FLASH/DATA_SPAWN 既有写法喵

### 7.【中】爆破钻组血 — 已修
- `src/main/java/com/blockdustry/building/BlastDrillRegistrar.java`
  - L40 `strength(6f)` → `strength(3.8f)`（→ 单格 10+10×3.8=48、组血 48×16=768 = 原版 16×40×1.2）
- `src/main/java/com/blockdustry/building/BlockdustryBlocks.java`
  - L396 组血注释 `1280（strength 6 → 单格 80）` → `768（strength 3.8 → 单格 48）`
- 整合清单文档 `docs/子agent/^P1_批1D_blastDrill整合清单.md` 3 处误写 1280 同步更正（L14/L73/L96）
- 注：BlastDrillBlock.java 实际无 strength 注释（审查报告提及，核对后发现无此注释，无需改）喵

### 8.【中】硅冶炼组血 — 已修
- `src/main/java/com/blockdustry/building/SiliconSmelterRegistrar.java`
  - L42 `strength(4f)` → `strength(3f)`（→ 单格 40、组血 160 = 原版，与 kiln strength3 同族一致）
- `src/main/java/com/blockdustry/building/BlockdustryBlocks.java`
  - L387 硅冶炼组血注释补全：`组血 160（strength 3 → 单格 40）`喵

## 编译验证喵
- 命令：JDK21 `javac` + classpath（build/classes + BlockHealth 子仓库 classes + 反混淆 Minecraft outputs.jar + neoforge universal + gradle 缓存库）
- 8 个改动文件编译 **EXIT 0 全过**（LaserDrillBlockEntity/LaserDrillBlockEntityRenderer/SiliconSmelterBlockEntityRenderer/KilnBlockEntity/FireBulletEntity/BlastDrillRegistrar/SiliconSmelterRegistrar/BlockdustryBlocks）喵

## 低危未修（记录）喵
- pneumatic rotator 用弧度非度数 + 无 timeDrilled 同步（PneumaticDrillBlockEntityRenderer.java:53）——不在 8 中危范围，未动
- 两套矿色表不一致（pneumatic mineColor vs blast oreColor）——未动
- ContainerBlockEntityRenderer.java:42 图标中心应为 (1.0,1.0)——未动
- KilnBlockEntityRenderer.java:106 白芯 alpha 用呼吸值 fa；L111 光晕 alpha 0.35→0.65——未动
- FuseBlockEntityRenderer.java:104 炮口闪光固定种子——未动
- 以上均非审查点名的 8 中危，且部分涉及 T31 fuse / 低危细节，留待后续喵
